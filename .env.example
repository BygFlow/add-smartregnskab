# ─────────────────────────────────────────────────────────────
#  ADD SmartRegnskab — miljøvariabler
#  Kopier denne fil til .env og udfyld med rigtige værdier.
#  ALdrig commit .env til git!
# ─────────────────────────────────────────────────────────────

# ── App ──
NODE_ENV=production
APP_BASE_URL=https://add-smartregnskab.onrender.com
VITE_API_BASE_URL=https://add-smartregnskab.onrender.com
ALLOWED_ORIGINS=https://add-smartregnskab.onrender.com,https://localhost
PORT=5000

# Første platformadministrator oprettes kun, hvis der endnu ikke findes en.
PLATFORM_ADMIN_EMAIL=admin@smartregnskab.dk
PLATFORM_ADMIN_PASSWORD=

# ── Database & filer ──
# I produktion: peg mod persistent disk
# DATABASE_PATH=/var/data/data.db
# FILE_STORAGE_DIR=/var/data/files
# BACKUP_DIR=/var/data/backups
DATABASE_PATH=data.db
FILE_STORAGE_DIR=./uploads

# ── Kryptering (KRITISK) ──
# Generer med: openssl rand -hex 32
ENCRYPTION_KEY=

# Demo-data må kun aktiveres eksplicit i lokal udvikling.
ALLOW_DEMO_SEED=0

# ── Stripe (betalinger) ──
# Hent på: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ── Email (Resend) ──
# Hent på: https://resend.com/api-keys
RESEND_API_KEY=
MAIL_FROM=noreply@smartregnskab.dk

# ── SMS (valgfrit) ──
SMS_API_URL=
SMS_API_KEY=

# ── MobilePay (valgfrit) ──
MOBILEPAY_CLIENT_ID=
MOBILEPAY_CLIENT_SECRET=
MOBILEPAY_WEBHOOK_SECRET=

# ── S3 filstorage (valgfrit) ──
S3_BUCKET=
S3_REGION=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=

# ── Android release-signering (sættes i CI, aldrig i kildekoden) ──
ANDROID_KEYSTORE_FILE=
ANDROID_KEYSTORE_PASSWORD=
ANDROID_KEY_ALIAS=
ANDROID_KEY_PASSWORD=
