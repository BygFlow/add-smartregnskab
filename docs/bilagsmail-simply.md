# Bilagsmail via Simply.com

## Status

- `bilag-system@addsmartregnskab.dk` er en dedikeret teknisk postkasse. Testvideresendelse fra `bilag-test@addsmartregnskab.dk` til postkassen er bekræftet.
- Webhook, selskabsspecifikke adresser, filopbevaring og en IMAP-bro findes i koden. Render-jobbet `add-smartregnskab-bilagsmail` blev oprettet 2026-09-23. Den 24. september blev en fiktiv PDF sendt til ADD SmartDrift ApS' unikke pilotadresse og modtaget én gang i den rigtige bilagsindbakke; en gentaget jobkørsel skabte ingen dublet. Bilaget blev ikke bogført.
- En isoleret lokal test med to fiktive virksomheder bekræfter korrekt selskabsadskillelse, afvisning af ukendt modtager, ingen dublet ved genforsøg og ingen automatisk bogføring af mailbilaget. Derudover er særskilt modtagelse efterfølgende verificeret i den levende app for ADD SmartDrift ApS og ADD Multi Service ApS med syntetiske test-PDF'er. Begge testfiler ligger nu gendanneligt i hver virksomheds papirkurv, ikke i den aktive indbakke.
- `DOCUMENT_INBOUND_READY` blev først holdt på `false` under piloten. Den generelle selskabsspecifikke modtagelse er efterfølgende aktiveret; nye virksomheder må kun få vist en adresse, hvis den tilhørende videresendelse hos Simply er verificeret eller oprettet. E-mailbilag kræver stadig manuel godkendelse før bogføring.
- Til en kontrolleret pilot kan `DOCUMENT_INBOUND_TEST_COMPANY_ID` sættes til **én** testvirksomheds interne id. Kun dens leder kan da se den tokenbaserede adresse i bilagsindbakken med en tydelig testadvarsel. Flaget åbner ikke adressen for andre virksomheder og ændrer ikke `DOCUMENT_INBOUND_READY`.
- Catch-all på hoveddomænet er ikke aktiveret og må ikke aktiveres som genvej. Den eksisterende testvideresendelse dækker kun én fast adresse; nye selskabsadresser leveres ikke automatisk til postkassen.
- `npm run mail:forward:simply` kan kontrollere én tokenbaseret selskabsadresse mod Simply og oprette dens videresendelse med `-- --apply`. Uden `--apply` læser den kun status. Appkoden kontrollerer/opretter en unik videresendelse, når virksomhedens bilagsindbakke åbnes. Simply API-nøglen blev den 24. september 2026 lagt som hemmelig miljøvariabel på Render-appens service. Den generelle aktivering og adskilte modtagelse i de to nævnte virksomheder er efterfølgende verificeret i appen; løbende overvågning af job, afviste mails og videresendelser er fortsat nødvendig.
- Simply-nøglen hører aktuelt til konto `S649349` under ADD Group ApS og kan teknisk tilgå hele den Simply-konto. Brugeren ønsker senere at flytte Simply-konto og domænet `addsmartregnskab.dk` til ADD SmartDrift ApS. Overdragelse og eventuel nøgleudskiftning skal planlægges særskilt; ejerskabet er ikke ændret under denne opsætning.

## Sikker driftsopsætning

1. Deploy appkode og databasemigrationer. Bekræft at appen starter og at eksisterende selskaber og bilag kan læses.
2. Vælg en dokumenteret routing for adresser på formen `bilag-<selskabstoken>@<domæne>` til maskinpostkassen. Simply.coms [API til videresendelser](https://api.simply.com/2/doc/) kan oprette én regel per selskab. Appen skal have `SIMPLY_API_KEY` og `SIMPLY_PRODUCT_HANDLE` som hemmelige miljøvariabler, når automatisk oprettelse skal aktiveres; det lokale hjælpeværktøj kræver desuden `DOCUMENT_FORWARD_ADDRESS`. Produktnavn og domæne skal matche, og kun `bilag-` plus 32 hextegn accepteres. Nøglen må ikke skrives i Git, terminalhistorik eller chat. API'et dokumenterer GET og POST for mailvideresendelser, men ikke automatisk sletning; derfor kræver afvikling af et selskab en særskilt oprydningsprocedure. Afklar API-adgang og driftsovervågning før generel åbning. Brug ikke testaliaset som om det var en individuel selskabsadresse.
3. Opret en separat periodisk jobtjeneste, der kører `npm run mail:poll:simply`. IMAP-scriptet kører kun én gang per start; det er ikke en permanent proces. Jobbet kræver Node.js, projektets installerede afhængigheder og udgående HTTPS/IMAPS.
4. Sæt `DOCUMENT_INBOUND_DOMAIN` og en stærk, tilfældig `DOCUMENT_INBOUND_WEBHOOK_SECRET` på appen. Sæt samme domæne og hemmelighed samt `DOCUMENT_INBOUND_API_URL=https://app.addsmartregnskab.dk`, `SIMPLY_DOCUMENT_IMAP_USER=bilag-system@addsmartregnskab.dk` og `SIMPLY_DOCUMENT_IMAP_PASSWORD` på jobbet. Brug hemmelige miljøvariabler; læg aldrig adgangskode eller webhook-hemmelighed i Git, logs eller chat.
5. Hold `DOCUMENT_INBOUND_READY=false`, mens testen udføres. Sæt pilot-id'et til testvirksomhedens id, og opret hos Simply én videresendelse fra netop dens viste tokenadresse til maskinpostkassen. Send en ufarlig PDF med entydigt testindhold. Bekræft i appens bilagsindbakke, at filen lander hos det rigtige selskab, og at en gentagen IMAP-kørsel ikke opretter en dublet.
6. Bekræft at mailbilaget **ikke** autobogføres, og at en bruger hos virksomheden kan gennemse og godkende det. Bekræft også at ugyldig signatur, forkert modtager og for store filer afvises.
7. Først når appen er deployet med Simply API-adgang, og oprettelse af videresendelser er verificeret for **nye** selskaber uden manuelle ad hoc-regler, sættes `DOCUMENT_INBOUND_READY=true`. Hver virksomhed får sit eget vedvarende token og sin egen videresendelse; `bilag-system@` bruges kun bagved. Overvåg fejlede API-kald, jobkørsler og mails, der forbliver ulæste i maskinpostkassen.

## Afgrænsning

IMAP-broen læser kun ulæste, ikke-flagede mails og accepterer PDF eller understøttede billeder på højst 7 MB. Beskeder uden præcis én gyldig selskabsadresse, med for stort eller ugyldigt bilag eller med en permanent afvisning fra modtage-API markeres med IMAP-flaget `\Flagged` og bliver liggende ulæste til manuel kontrol. Dermed kan de ikke blokere senere gyldige mails. Efter en korrektion kan en operatør fjerne flaget for at prøve igen. Midlertidige API-fejl flagges ikke og forsøges automatisk igen. Et accepteret bilag markeres først læst, når appen har bekræftet modtagelsen. E-mailafsender er ikke identitetsbevis; derfor skal mailbilag altid godkendes af virksomheden før bogføring.
